# A股策略分析

**融合策略** + 长线持有，含分红回测、SQLite本地缓存、Web界面。

## 安装

```bash
pip install -r requirements.txt
```

PEP 668 限制（Debian/Ubuntu）：

```bash
pip install -r requirements.txt --break-system-packages
```

可选：中文字体（图表中文显示）

```bash
sudo apt install fonts-wqy-zenhei
```

## 策略

### 融合策略

MACD核心(40%) + 多因子(30%) + 价格位置/趋势(15%) + 量能(15%)。

**评分公式：**

```
S = M × 0.40 + F × 0.30 + P × 0.15 + Q × 0.15
```

| 维度 | 权重 | 评分规则 |
|------|:--:|------|
| **M** (MACD核心) | 40% | DIF/DEA 多空状态 \| 零轴位置 \| DIF↑₅/↓₅ \| BAR↑₃ \| 前高/前低动能背离 |
| **F** (多因子) | 30% | RSI[30,65]+10 / <30超卖+15 / >80超买−15 \| KDJ 多空状态 \| J<0+8 \| 布林下轨+12/上轨−8 \| WR>80+8 |
| **P** (价格位置/趋势) | 15% | P₂₅₀<25%+20 / P₂₅₀<40%+10 / P₂₅₀>80%−15 \| 年价格涨幅强弱；不代表财务基本面 |
| **Q** (量能) | 15% | OBV 5日净量能流 >40% +12 / <-40% −12 \| 量价背离−18 \| 年化波动率代理 |

**质量门禁（任一项触发即否决买入）：**

| 门禁 | 条件 |
|------|------|
| OBV净量能流 | 5日净量能流 < −60% |
| RSI超买 | RSI > 92 |
| 双弱信号 | M < 35 且 F < 40 |

**交易规则：**

| 方向 | 条件 |
|:--:|------|
| 🟢 买入 | 收盘满足 S ≥ 65 且通过门禁，下一交易日开盘执行 |
| 🔴 卖出 | 止损−8% \| 止盈+25% \| S<35 \| 获利回吐(S<50且盈利>12%) \| 顶背离 \| M顶/头肩顶破颈线 |

回测按买入佣金 $0.03\%$、卖出佣金 $0.03\%$、卖出印花税 $0.05\%$ 计算；总收益为顺序全仓交易的复利收益。

**经典形态（回测卖出专用）：**

| 形态 | 已破颈线 |
|------|:--:|
| 双头(M顶) | 强制卖出 |
| 头肩顶 | 强制卖出 |
| 看跌吞没 | 强制卖出 |

**批量回测结果（固定 100 只缓存股票，2026-07-19）：**

| 板块 | 数量 | 🟢买入 | 均胜率 | 均价差复利（不含分红） |
|------|:--:|:--:|:--:|:--:|
| 芯片 | 39 | 15 | 46.0% | +158.9% |
| 资源 | 30 | 11 | 48.8% | +68.9% |
| 消费 | 30 | 18 | 47.9% | -5.1% |
| 金融 | 1 | 1 | 52.5% | +2.5% |
| **总计** | **100** | **45** | **47.5%** | **+81.1%** |

均胜率为各股票胜率的等权平均；均价差复利为各股票顺序复利收益的等权平均，不含分红。该口径较旧版 99 只样本的 45.0% 均胜率提高 2.5 个百分点。

### 长线持有策略

买入持有不动，展示价差收益、累计分红、股息率走势。

- **买入**：数据起始日一次性买入
- **卖出**：不卖出，持有至今
- **分红**：持有期间所有已实施分红累加

## 分红回测

自动拉取 A 股历史分红数据（送股、转增、派息），计算每笔交易持股期间到手的每股分红金额。

- 数据源：新浪财经 F10
- 回测交易表显示每笔交易的价差收益 + 分红收益（元/股）
- 分红历史表展示历年派息方案与股息率

## 本地缓存

首次查询某只股票时从网络拉取全量数据并存入 SQLite，之后秒读。

- 数据库：`stock_cache.db`（已加入 .gitignore）
- K 线和分红数据均缓存
- 所有新拉取 K 线均为不复权口径，现金分红与送转单独计入。若某只股票的缓存来自旧版前复权后备源，请删除该股票的旧缓存后重新拉取。

## 测试

```bash
python3 -m unittest discover -s unittests -v
```

覆盖 engine（MACD/背离/融合回测/预测/多因子）、fetcher（不复权后备源、分红/送转边界）、db（缓存增删改查），共53个测试用例。

## 命令行

```bash
./run_cli.py 603893              # MACD择时
./run_cli.py multi 600329        # 多因子共振
./web.py 8099                    # Web (默认8080)
python3 batch_backtest.py        # 批量回测100只股票
```

## Web 界面

```bash
./web.py 8099
```

浏览器打开 `http://localhost:8099`：

- 输入6位股票代码
- 选择策略：融合策略 / 长线持有
- 勾选"已持仓"后可选择"计算分红"
- 融合策略显示：四维评分卡 + 详细分析过程 + 门禁检查 + 三面板图表(价格/MACD/OBV)

## 文件结构

```
a_shares_predict/
├── run_cli.py          # CLI入口
├── web.py              # Web界面（融合策略路由 + 图表）
├── engine.py           # 核心引擎（MACD/多因子/融合策略/形态检测/OBV/ATR）
├── fetcher.py          # 数据层（K线抓取、分红抓取、多源降级）
├── db.py               # SQLite缓存模块
├── batch_backtest.py   # 批量回测工具
├── unittests/          # 单元测试 (43个用例)
├── templates/
│   └── page.html       # Web页面模板
├── requirements.txt
├── CLAUDE.md           # AI开发文档
└── README.md
```
