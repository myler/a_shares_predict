# A 股多维策略研究 — Pareto34 风险预算 v1 维护约定

## 当前状态

- 运行说明、策略边界和文件用途统一维护在 [README.md](README.md)，不在此重复整套规则或历史收益。
- 当前版本 `pareto_daily_atoms_risk_v1`：34 个独立三值原子、同日全部 F1、风险预算、单股独立账户；Web 默认 `pareto`，只读已完成的历史报告，不是实时推荐。
- 2026-09-10 本轮实证失败，未完成用户的保本盈利目标。资金结论只认完整输出及全量账户核对，不能把实现完成、训练选优或引用文献当作盈利证据。
- [AUDIT_2026-09-09.md](AUDIT_2026-09-09.md) 是固定提交的历史审计，不代表全部缺陷已修复。
- 已删除因子回测 v1–v7、资金回测 v1–v3、旧 Web 融合渲染及跟踪日志；需要中间版本时从 Git 恢复，不复制多份编号脚本。

## 入口边界

- [pareto_strategy.py](pareto_strategy.py) 负责原子、非支配层和仓位风险计算；[capital_account.py](capital_account.py) 负责现金/股份、整手、T+1、费用、权息与回撤状态，不访问数据。
- [pareto_backtest.py](pareto_backtest.py) 负责快照、同日前沿、连续账户、分段净值和汇总；[pareto_web.py](pareto_web.py) 只读完成产物并核对资金恒等式，不刷新、不重跑、不回落旧加权。
- [web.py](web.py) 的 `/analyze` 默认 `pareto`，`/pareto-summary` 是全量总览，N 仅按代码顺序截断展示。目录仅由服务端 `PARETO_RUN_DIR` 指定，不接受 HTTP 路径；持仓/分红开关不用于 Pareto 历史账户。
- [templates/page.html](templates/page.html) 的 Pareto 首页文案读取模型默认值，报告读取本轮冻结所选参数；`RiskConfig` 默认波动目标 15%，本轮训练选择 10%，不可混称。
- 旧加权 `comprehensive` 显式保留：[engine.py](engine.py) 四维 40/30/15/15，$S^*=S-0.08A$；无资金流 Z、龙虎榜双路径或分级仓位。旧页面阈值仍读取旧回测默认参数。
- [run_cli.py](run_cli.py) `predict` 仍为旧加权预测；直接代码及 `chart`/`backtest`/`json` 为旧 MACD，`multi` 为旧多因子，均非 Pareto34。
- 旧 Web `/scan` 使用 [scan_composite.py](scan_composite.py) → [factors.py](factors.py)，是加权横截面相对分、可能联网取报价，不是 S* 或 Pareto34。它与默认多维总览分开保留。
- [money_backtest4.py](money_backtest4.py)、[money_scan_backtest.py](money_scan_backtest.py)、[money_pareto_backtest.py](money_pareto_backtest.py) 是待修研究实验，不把其收益当已验证结论，不自动接入 Web。
- [api.py](api.py) 默认仍为旧加权 `comprehensive`，无 Pareto34 JSON 接口；成交价/汇总与旧 Web 也有差异。小程序是独立旧实现，不宣称新旧入口一致。
- [fundamentals.py](fundamentals.py) 仅财务初筛，不参与新旧技术评分，不产生目标价或买卖信号；长线展示仍有送转及日期口径缺口，不作为已验证基准。

## 多维与账户契约

- 保持 34 个三值坐标分别比较，不聚合回四维或辅助共识。“独立”不等于统计独立，三值化丢失幅度；不能声称保留全部信息。相同向量同层，高维前沿不提供唯一最优解。
- 仅比较同日有效、预热完成且通过门禁的股票，不前填信号；全部 F1 保留但不等于满仓。当前运行器只取 F1，不推断其他标签的精确层次。
- 波动率/历史 ES 上限定风险仓位，成本跟踪仅优化仓位，不给 34 信号加权；运行器以 `risk_cap=desired` 约束仓位，零目标硬退出不能被费用覆盖。
- 每只股票独立 100 万元，不跨账户借资/归一化、闲置现金零利息；失败、隔离、晚起始、陈旧及无交易账户均保留全部本金。质量类别可交叉，不能相加作排除数。
- 收盘信号→下一市场日开盘尝试成交，权息先于当日订单入账；分段不重置账户或清仓。最后 5 市场日禁买并尝试退出，无法清仓保留残值，不拿最后可用价当现金。
- 核对权益＝现金＋残值、盈亏＝权益−全部本金。费用已扣、现金分红已入账，不重复加减；滑点已在成交价中，显式费用不含滑点。分红用元/权息前持有股，不与复权收益重复计数。
- 15% 回撤退出及 20 市场日冷静期不是保本/最大回撤保证；日线涨跌停代理、无参与率限制、权息到账/股份可用日、红利税及零碎股近似均须公开。
- 配股未建模、权息错误/未核验账户全期禁买并保留现金；未解释>25%开盘跳空后禁新信号。这是事后质量隔离，不是历史时点可投资股票池，也不证明权息完整。

## 数据与运行安全

- 使用本项目独立虚拟环境；核心/Web 依赖 [requirements.txt](requirements.txt)，研究采集依赖 [requirements-research.txt](requirements-research.txt)。不默认修改系统 Python。
- Windows 终端通过 Ubuntu01 WSL 执行，工作目录为仓库根目录。
- Pareto 行情只读备份源库，不刷新生产缓存。[action_fallback.py](action_fallback.py) 的 `resolve_action_snapshot()` 为运行器解析权息：`--action-snapshot` 可复用覆盖匹配的完整 EM 冻结目录；省略则自动批量冻结 EM，不调用新浪下载、不冲击新浪限流。
- 冻结完整时不发 HTTP；不完整时可能继续 EM 公开请求。拒绝/限流保留证据，不绕过或自动重试。保留 [action_snapshot.py](action_snapshot.py) 的独立新浪工具及既有成功快照，EM 配股交叉核验另存结果，不覆盖原证据，不宣称双源分红完全一致。
- 运行目录冻结行情、股票池、日历和参数；改变数据版本用新目录，不借续跑静默换样本。运行会重建完成汇总，读报告应等待完成标记；`--cached-actions` 的未核验缓存不能代替完整权息核验。
- 旧行情路径仍是缓存 → 新浪 → 腾讯不复权 → 东方财富不复权；腾讯适配器字段错位待修，不能说新账本已修复全部旧数据入口。
- 普通缓存不自动刷新，强制刷新可能截短历史。保留数据血缘/快照后再迁移，不随意清库。
- [collect_market_data.py](collect_market_data.py) 的 lhb 模式和 [collect_lhb_10y.py](collect_lhb_10y.py) 有无条件 DROP，审查时不能运行这些入口。
- [db.py](db.py) 的读函数会调用读写建表连接。严格只读审查使用 SQLite URI `mode=ro`，不调用生产初始化/刷新。
- 测试使用临时数据库与模拟网络；全套验证可再统一重定向临时数据库并阻止联网，不能访问真实缓存。
- 不复权价成交与现金分红/送转独立记账，权息连续化价格仅供信号。新账本仍有已披露近似，旧长线及融合持仓风控仍有缺口，不宣称完整总收益已验证。

## 维护习惯

- 先修输入契约、因果时序和现金/股份账本，再调参或全量回测。
- 不从“未被 Web 导入”推导脚本废弃；保留仍有独立用途的入口和数据工具。
- 修改策略时同步 README、相应新旧 Web 文案与契约测试；旧“增持/减持”只是评分标签，新账户动作以真实账本记录为准。
- 回测注明请求/有效截止、市场日数、全量本金、数据版本、费用、权息、残值、不可成交与资金占用；优先公开失败及高频 F1 切换/订单成本问题，不隐藏无交易账户或只报盈利样本。
- 训练截至 2021-12-31，验证为 2022～2023，2024 年起是设计上的留出分段；代码仅按训练盈亏选择 10%/15%/20% 波动目标，但本轮多次看过后段结果，不是完全独立盲测。看过后再调参须另记版本，不冒充新的 holdout。
- Moreira–Muir（NBER w22208）及 cvxportfolio 仅作波动管理、风险/成本优化的概念参考，不为本策略公式、参数或盈利背书。
- 报告文件可见后再添加链接，不预链接计划文件；历史审计及其固定提交链接保持不变。
- 用户偏好务实直接、准确数据；收益关注价差、现金分红、总收益，分红用元/股，优先单年收益而非仅年化。
