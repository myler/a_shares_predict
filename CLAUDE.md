# A股策略分析 — 项目说明

## 项目概述
A股量化分析工具，Web端仅保留融合策略 + 长线持有。MACD择时、多因子共振代码保留在 engine.py 中但 Web 端已隐藏。

## 架构
```
run_cli.py        → CLI入口，仅参数解析+格式化输出，不写逻辑
web.py            → Web入口，HTTP服务，HTML模板在 templates/page.html
engine.py         → 核心引擎：MACD/多因子/融合策略/形态检测/OBV/ATR
fetcher.py        → 数据层：K线多源降级抓取、分红抓取、股票名称
db.py             → SQLite缓存（stock_cache.db），K线和分红均缓存
batch_backtest.py → 批量回测工具，99只股票分板块
```

## 关键约定
- PEP 668 环境，pip install 需加 `--break-system-packages`
- 数据源优先级：新浪 → 腾讯(前复权) → 东方财富(前复权)
- 新浪API无复权参数，返回不复权价格
- DB 已加入 .gitignore，其他机器 clone 后自动重建
- 字体文件 wqy-zenhei.ttf 可选，缺失时图表无中文但不崩溃

## 核心概念
- **复权**：分红后股价会除权跳空。前复权把历史价格下调使曲线连续。本项目用不复权价格+累加分红的方案，更直观
- **股息率 vs 年化**：股息率=每股分红/股价(快照)；年化=多年总收益折成年均。用户不认年化，股票看单年收益
- **分红显示**：用绝对值(元/股)而非百分比，因为分红按持股数派息，与买入价无关

## 策略规则

### 融合策略（当前主推）
- **设计理念**：吸收 MACD 择时的高胜率基因 + 多因子的确认能力 + 基本面估值过滤
- **权重**：MACD核心 40% + 多因子 30% + 基本面 15% + 量能 15%
- **评分公式**：S = M×0.40 + F×0.30 + V×0.15 + Q×0.15
- **MACD核心 (M)**：金叉/零轴/DIF斜率/BAR趋势/底背离/顶背离
- **多因子 (F)**：RSI(14)/KDJ(9,3,3)/Bollinger(20,2)/WR(10)
- **基本面 (V)**：250日百分位估值 + 年增长趋势
- **量能 (Q)**：OBV趋势 + 量价背离

**门禁**：OBV<0.90否决 | RSI>92否决 | 双弱(M<35∧F<40)否决

**买卖规则**：
- 买入：S ≥ 65 ∧ 通过门禁
- 卖出：止损−8% | 止盈+25% | S<35 | 获利回吐(S<50且盈利>12%) | 顶背离 | 形态破颈线

**99只批量回测**：均胜率 45.0%，均收益 +84.2%，51只买入信号

### MACD择时策略（代码保留，Web隐藏）
- 买入：底背离(熊市抄底) | 零轴上金叉(牛市追涨)
- 卖出：顶背离(牛市见顶) | 死叉(熊市止损)
- 牛熊：价格>MA60 且 MA60(10日斜率)>0 → 牛市，连续5日确认切换
- 评分：金叉+2/死叉-2 | 零轴上+1/下-1 | 牛+2/熊-2 | DIF↑+1/↓-1 | 顶背离-3

### 多因子共振策略（代码保留，Web隐藏）
- 指标：RSI(14) + KDJ(9,3,3) + Bollinger(20,2) + WR(10) + MA20 + 量能
- 买入：score≥2 且 RSI<75；卖出：score≤-3 | 获利>8%回吐 | 止损-6% | 止盈+15%

### 长线持有策略
- 数据起始日一次性买入，持有至今不卖
- Web端选中此策略自动锁定"已持仓"

### 经典形态检测
- `detect_classic_patterns()` — 全量检测（预测用）
- `_quick_pattern_sell()` — 快速检测（回测用，每5天一次）
- 双头(M顶): 颈线已破 -25 / 未破 -15
- 头肩顶: 颈线已破 -30 / 未破 -20
- 三白兵 +15 | 晨星 +15 | 看跌吞没 -15

### 辅助指标
- `calc_obv()` — OBV能量潮，用于量价背离检测
- `calc_atr()` — ATR平均真实波幅

## 图表性能
- 图表降采样：>1000点自动降到~800点
- DPI: 90，尺寸: 14×8英寸
- 综合策略响应: ~2.5s（含回测+预测+图表）
- matplotlib纯CPU渲染，WSL无GPU加速

## 用户风格
- 务实直接，不接受模糊话术或过度修辞
- 数据准确性要求极高，发现错误会直接指出
- 偏好绝对值(元/股)而非百分比显示分红
- 股票分析时关注价差收益、分红收益、总收益三项

## 运行
```bash
./run_cli.py 603893              # CLI MACD
./run_cli.py multi 600329        # CLI 多因子共振
./web.py 8099                    # Web (默认8080)
python3 batch_backtest.py        # 批量回测99只股票
python3 -m unittest discover -s unittests -v  # 43个测试
```

## GitHub
https://github.com/myler/a_shares_predict (private)
