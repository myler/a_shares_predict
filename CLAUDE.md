# A 股多维策略研究 — Pareto34 风险预算 v1 维护约定

## 当前状态

- 运行说明、策略边界和文件用途统一维护在 [README.md](README.md)，不在此重复整套规则或历史收益。
- 当前版本 `pareto_daily_atoms_risk_v1`：34 个独立三值原子、同日全部 F1、风险预算、单股独立账户。Web 两标签为「策略分析」／「融合策略回测总览」；第一标签默认「融合策略」（`pareto`）每次联网刷新本股后作条件诊断，第二标签只读完成的历史报告，不混称只读或盘中实时推荐。
- 2026-09-10 本轮实证失败，未完成用户的保本盈利目标。资金结论只认完整输出及全量账户核对，不能把实现完成、训练选优或引用文献当作盈利证据。
- [AUDIT_2026-09-09.md](AUDIT_2026-09-09.md) 是固定提交的历史审计，不代表全部缺陷已修复。
- 已删除因子回测 v1–v7、资金回测 v1–v3、旧 Web 融合渲染及跟踪日志；需要中间版本时从 Git 恢复，不复制多份编号脚本。

## 入口边界

- [pareto_strategy.py](pareto_strategy.py) 负责原子、非支配层和仓位风险计算；[capital_account.py](capital_account.py) 负责现金/股份、整手、T+1、费用、权息与回撤状态，不访问数据。
- [pareto_backtest.py](pareto_backtest.py) 负责快照、同日前沿、连续账户、分段净值和汇总；[pareto_web.py](pareto_web.py) 只读完成产物并核对资金恒等式，不刷新、不重跑、不回落旧加权。
- [web.py](web.py) 的 `/analyze` 只接受「融合策略」（`pareto`，默认）／「长线持有」（`buyhold`）／「巴芒基本面研究」（`value`）三个选项；`comprehensive`、MACD、`multi` 旧 Web 分派与方法已删除，旧策略值返回 400，`/scan` 返回 404，不是仅隐藏。
- 第一标签融合链为 [fusion_web.py](fusion_web.py) → [fusion_analysis.py](fusion_analysis.py) → [live_data.py](live_data.py) 的 `refresh_stock()`：先刷新目标股票，再算共享原子／门禁／风险和精确同日比较；HTML 不重复刷新。[fusion_chart.py](fusion_chart.py) 仅内存绘制三面板图，不取数据或写输出文件。
- `/pareto-summary` 是只读全量总览，N 仅按代码顺序截断展示，不影响全部本金。模板内部 `tab-scan`／`runScan()` 仅请求此路由。目录仅由服务端 `PARETO_RUN_DIR` 指定，不接受 HTTP 路径；缺少完成报告只提示不可用，不自动回放。
- [pareto_web.py](pareto_web.py) 的 `render_stock_report()` 仍是内部只读历史 helper，不由第一标签 HTTP 调用。当前分析附历史摘要但不改历史账户；历史缺失可继续当前诊断，须披露无历史收益／截面，不能用全量总收益冒充个股结果。
- [templates/page.html](templates/page.html) 首页方法说明读取模型默认值；完整历史报告及有完整报告的当前分析采用冻结所选参数。本轮训练选择 10%，`RiskConfig` 默认波动目标 15%；报告缺失时当前分析明确使用默认 15%，不可混称。
- 旧加权 `comprehensive` 仅在独立引擎／CLI／API 保留：[engine.py](engine.py) 四维 40/30/15/15，$S^*=S-0.08A$；无资金流 Z、龙虎榜双路径或分级仓位。当前 Web 没有该策略或旧页面阈值。
- [run_cli.py](run_cli.py) `predict` 仍为旧加权预测；直接代码及 `chart`/`backtest`/`json` 为旧 MACD，`multi` 为旧多因子，均非 Pareto34。
- [scan_composite.py](scan_composite.py) → [factors.py](factors.py) 独立海选 CLI／研究用途保留，是加权横截面相对分、可能联网取报价，不是 S* 或 Pareto34；移除 Web `/scan` 不等于删除脚本。
- [money_backtest4.py](money_backtest4.py)、[money_scan_backtest.py](money_scan_backtest.py)、[money_pareto_backtest.py](money_pareto_backtest.py) 是待修研究实验，不把其收益当已验证结论，不自动接入 Web。
- [api.py](api.py) 默认仍为旧加权 `comprehensive`，无 Pareto34 JSON 接口，也不走新融合刷新链；未传 `opens`、逐笔价差简单相加的旧问题仍在，不与当前 Web 历史账户混称。小程序是独立旧实现，不宣称新旧入口一致。
- [fundamentals.py](fundamentals.py) 仅财务初筛，不参与新旧技术评分，不产生目标价或买卖信号；长线展示仍有送转及日期口径缺口，不作为已验证基准。

## 当前单股分析契约

- 每次有效请求先刷新目标股票，只刷新本股，不刷新全市场；行情全部来源失败则暂不下结论，不回落缓存、旧加权或历史图。`refresh=0`／`force=0` 不能禁用第一标签刷新；只读／禁网任务不得访问该分析路由。
- 用回测同一 34 原子、资格／门禁、因果权息连续价格、波动率／历史 ES；不算 $S^*$。只能与冻结池精确同日有效股票比较，不把旧 peer 末行填到今天、不沿用旧 F1、不让单股自封前沿。新行情超出冻结日历／本轮有效截止 2026-09-08 时 F1 未知，不能给建仓／持有候选，也不能称取得今日全市场截面。
- 历史有效截止不能截短本次新行情；最后 5 市场日退出规则与旧账户仓位／回撤状态不用于当前持仓。条件风险上限不是执行目标，F1 未知不能作为买入依据。
- 「已持仓」可点，下一次分析改变建仓／持有候选、不买入／退出信号的条件解释；仅布尔值，无数量、成本、净值高点和冷静期，不能给实际增减仓或个人账户回撤退出建议。
- UI 已持仓时显示「计算分红」，勾选后附本次新权息每股表；渲染器允许独立展示。必要权息核验不受开关影响，历史总收益与已入账股息也不受开关影响。近 12 个日历月现金简单合计为元／各次权息前股，不是统一初始股收益或股息率。
- 结果先给 ✅／❌／⚠️ 条件符号并说明非盈利概率，再列当前＋历史摘要，分别标核验时间、实际行情日、历史请求／有效截止；34 详情分 M6/F5/P2/Q3/Aux18，随后历史年度与最近 20 次实际 filled 事件（非实盘），最后价格／权息连续 MACD／34 行热图，不设聚合评分面板。
- 图中仅标可用冻结账本成交，历史截止后无新成交标记；仅热图可抽样至最多 1000 列并披露。权息失败保留带警告的原价诊断，不给买卖结论；绘图失败不拿旧图替代、不重刷。

## 多维与账户契约

- 保持 34 个三值坐标分别比较，不聚合回四维或辅助共识。“独立”不等于统计独立，三值化丢失幅度；不能声称保留全部信息。相同向量同层，高维前沿不提供唯一最优解。
- 仅比较同日有效、预热完成且通过门禁的股票，不前填信号；全部 F1 保留但不等于满仓。当前运行器只取 F1，不推断其他标签的精确层次。
- 波动率/历史 ES 上限定风险仓位，成本跟踪仅优化仓位，不给 34 信号加权；运行器以 `risk_cap=desired` 约束仓位，零目标硬退出不能被费用覆盖。
- 每只股票独立 100 万元，不跨账户借资/归一化、闲置现金零利息；失败、隔离、晚起始、陈旧及无交易账户均保留全部本金。质量类别可交叉，不能相加作排除数。
- 收盘信号→下一市场日开盘尝试成交，权息先于当日订单入账；分段不重置账户或清仓。最后 5 市场日禁买并尝试退出，无法清仓保留残值，不拿最后可用价当现金。
- 核对权益＝现金＋残值、盈亏＝权益−全部本金。费用已扣、现金分红已入账，不重复加减；滑点已在成交价中，显式费用不含滑点。分红用元/权息前持有股，不与复权收益重复计数。
- 15% 回撤退出及 20 市场日冷静期不是保本/最大回撤保证；日线涨跌停代理、无参与率限制、权息到账/股份可用日、红利税及零碎股近似均须公开。
- 配股未建模、权息错误/未核验账户全期禁买并保留现金；未解释>25%开盘跳空后禁新信号。这是事后质量隔离，不是历史时点可投资股票池，也不证明权息完整。

## 数据与运行安全

- 使用本项目独立虚拟环境；核心/Web 依赖 [requirements.txt](requirements.txt)，研究采集依赖 [requirements-research.txt](requirements-research.txt)。不默认修改系统 Python。
- Windows 终端通过 Ubuntu01 WSL 执行，工作目录为仓库根目录。
- **用户已明确授权第一标签融合分析每次联网并定向更新目标股票数据库，取代该入口原先只读约束**；仅此刷新链适用，不授权全市场刷新、清库、其他表更新或冻结报告重写。文档／审查任务仍按各次请求的禁网限制执行。
- [live_data.py](live_data.py) 先用 `mode=ro` 保存目标旧行情与元数据，冻结原始响应和 SHA256；验证后 `BEGIN IMMEDIATE` 重查并发变更，仅对目标行情／股票元数据 UPSERT，保留窗口外历史，提交后读取完整目标历史。不初始化表、不写旧分红缓存。权息失败不撤销已提交行情，提交后 I/O 失败须披露 `price_committed`，不能假称回滚。
- `checked_at` 是带时区核验时间，不是报价日期；上海 15:00 前过滤当日临时日线，拒绝未来／无效数据，来源末日不得早于缓存真实 `MAX(date)`。同日核验可修正但不称新增日；未核验交易日历／停牌，不能把来源旧末日叫今日行情。
- 成交量有历史时排除缓存末日，至少 3 对正量重叠日须在 1% 内一致支持 1／100／0.01 唯一倍数；单位未知、混杂或不足拒绝来源。无历史按已知股／手转股，有历史保持缓存单位，不静默重标全库。
- 每次行情刷新成功后重新按代码请求 EM 有日期分红、全部未注明除权日的已实施分红、无日期过滤的配股生命周期数据；核验分页与覆盖，不复用旧市场权息快照，不调用新浪权息。权息失败、未知／存在配股禁止信号；拒绝／限流保留证据并停止后续权息请求，不重试或绕过。
- EM 空响应只接受第一页同时满足整数 `code=9201`、`success=false`、显式 `result=null`、精确「返回数据为空」；规范化空结果但保留原始字节／SHA256／页证据。其他错误或后续页哨兵不当空；来源无记录不证明历史无事件。
- **Pareto 回测**行情仍只读备份源库，不刷新生产缓存。[action_fallback.py](action_fallback.py) 的 `resolve_action_snapshot()` 为运行器解析权息：`--action-snapshot` 可复用覆盖匹配的完整 EM 冻结目录；省略则自动批量冻结 EM，不调用新浪下载、不冲击新浪限流。这与第一标签逐股新请求分开。
- 冻结完整时不发 HTTP；不完整时可能继续 EM 公开请求。拒绝/限流保留证据，不绕过或自动重试。保留 [action_snapshot.py](action_snapshot.py) 的独立新浪工具及既有成功快照，EM 配股交叉核验另存结果，不覆盖原证据，不宣称双源分红完全一致。
- 运行目录冻结行情、股票池、日历和参数；改变数据版本用新目录，不借续跑静默换样本。运行会重建完成汇总，读报告应等待完成标记；`--cached-actions` 的未核验缓存不能代替完整权息核验。
- 旧行情路径仍是缓存 → 新浪 → 腾讯不复权 → 东方财富不复权；[fetcher.py](fetcher.py) 腾讯数组已按 date/open/close/high/low/volume 修正 OHLC 映射，不再列为当前待修错位，也不据此宣称全部旧入口已修复。
- 旧普通缓存不自动刷新，旧 `fetch_kline(force_refresh=True)` 仍可能替换并截短历史，未全面修复；不能套用新 `live_data` 的保留历史保证。保留数据血缘／快照后再迁移，不随意清库。
- [collect_market_data.py](collect_market_data.py) 的 lhb 模式和 [collect_lhb_10y.py](collect_lhb_10y.py) 有无条件 DROP，审查时不能运行这些入口。
- [db.py](db.py) 的读函数会调用读写建表连接。严格只读审查使用 SQLite URI `mode=ro`，不调用生产初始化/刷新。
- 测试使用临时数据库与模拟网络；全套验证可再统一重定向临时数据库并阻止联网，不能访问真实缓存。
- 不复权价成交与现金分红/送转独立记账，权息连续化价格仅供信号。新账本仍有已披露近似，旧长线及融合持仓风控仍有缺口，不宣称完整总收益已验证。

## 维护习惯

- 先修输入契约、因果时序和现金/股份账本，再调参或全量回测。
- 不从“未被 Web 导入”推导脚本废弃；保留仍有独立用途的入口和数据工具。
- 修改策略时同步 README、当前 Web 文案与契约测试，不恢复已移除旧 Web 策略；旧 CLI／API 的“增持/减持”只是评分标签，当前候选不是实际调仓，历史账户动作以真实账本记录为准。
- 新增刷新、分析、渲染、图表测试及更新的 Web／fetcher 契约职责见 [README.md](README.md)。328 项是改版前验证；2026-09-11 完整隔离验证 470 项通过，真实单股刷新验收另行执行，不等于联网测试或全市场数据验证。检查文档断言中的旧入口字符串，不为满足过时断言恢复错误文案。
- 回测注明请求/有效截止、市场日数、全量本金、数据版本、费用、权息、残值、不可成交与资金占用；优先公开失败及高频 F1 切换/订单成本问题，不隐藏无交易账户或只报盈利样本。
- 训练截至 2021-12-31，验证为 2022～2023，2024 年起是设计上的留出分段；代码仅按训练盈亏选择 10%/15%/20% 波动目标，但本轮多次看过后段结果，不是完全独立盲测。看过后再调参须另记版本，不冒充新的 holdout。
- Moreira–Muir（NBER w22208）及 cvxportfolio 仅作波动管理、风险/成本优化的概念参考，不为本策略公式、参数或盈利背书。
- 报告文件可见后再添加链接，不预链接计划文件；历史审计及其固定提交链接保持不变。
- 用户偏好务实直接、准确数据；收益关注价差、现金分红、总收益，分红用元/股，优先单年收益而非仅年化。
